## -*- c -*-
######################################################################
## Do the .h file
######################################################################
@open NASCenter_ICP_oid.h@
/*
 * Note: this file originally auto-generated by mib2c using
 *        $Id: mib2c.old-api_ICP.conf,v 1.1 2003/12/17 07:51:34 baravind Exp $
 */
#ifndef $name.uc_H
#define $name.uc_H

#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* 
 * ${name}_variables_oid:
 *   this is the top level oid that we want to register under.  This
 *   is essentially a prefix, with the suffix appearing in the
 *   variable below.
 */

oid ${name}_variables_oid[] = { $name.commaoid };

size_t ${name}_oid_len = OID_LENGTH(${name}_variables_oid);

@foreach $i table@
 @foreach $c column@
    @eval $magic = $magic + 1@
    @if $c.settable@
oid ${c}_variables_oid[] = { $name.commaoid,1,$c.subid};
size_t ${c}_oid_len = OID_LENGTH(${c}_variables_oid);
    @end@
    @if !$c.settable@
oid ${c}_variables_oid[] = { $name.commaoid,1,$c.subid};
size_t ${c}_oid_len = OID_LENGTH(${c}_variables_oid);
    @end@
 @end@

#endif /* $name.uc_H */
