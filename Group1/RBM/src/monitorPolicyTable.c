/*
 * Note: this file originally auto-generated by mib2c using
 *       : mib2c.array-user.conf,v 5.15.2.1 2003/02/27 05:59:41 rstory Exp $
 *
 * $Id: monitorPolicyTable.c,v 1.7 2004/01/05 05:40:33 saisk Exp $
 *
 *
 * For help understanding NET-SNMP in general, please check the 
 *     documentation and FAQ at:
 *
 *     http://www.net-snmp.org/
 *
 *
 * For help understanding this code, the agent and how it processes
 *     requests, please check the following references.
 *
 *     http://www.net-snmp.org/tutorial/
 *
 *
 * You can also join the #net-snmp channel on irc.openprojects.net
 *     and ask for help there.
 *
 *
 * And if all else fails, send a detailed message to the developers
 *     describing the problem you are having to:
 *
 *    net-snmp-coders@lists.sourceforge.net
 *
 *
 * Yes, there is lots of code here that you might not use. But it is much
 * easier to remove code than to add it!
 */

#include <errno.h>

#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

#include <net-snmp/library/snmp_assert.h>

#ifdef WIN32
#include "../inc/monitorPolicyTable.h"
#else
#include "monitorPolicyTable.h"
#endif

static     netsnmp_handler_registration *my_handler = NULL;
static     netsnmp_table_array_callbacks cb;

oid monitorPolicyTable_oid[] = { monitorPolicyTable_TABLE_OID };
size_t monitorPolicyTable_oid_len = OID_LENGTH(monitorPolicyTable_oid);

void RBM_Load_Container_From_File (void);
#ifdef monitorPolicyTable_IDX2
/************************************************************
 * keep binary tree to find context by name
 */
static int monitorPolicyTable_cmp( const void *lhs, const void *rhs );




/************************************************************
 * compare two context pointers here. Return -1 if lhs < rhs,
 * 0 if lhs == rhs, and 1 if lhs > rhs.
 */
static int
monitorPolicyTable_cmp( const void *lhs, const void *rhs )
{
    /*
     * check primary key, then secondary. Add your own code if
     * there are more than 2 indexes
     */
    int rc;

    /*
     * TODO: implement compare. Remove this ifdef code and
     * add your own code here.
     */
#ifdef TABLE_CONTAINER_TODO
    snmp_log(LOG_ERR,
             "monitorPolicyTable_compare not implemented! Container order undefined\n" );
    return 0;
#endif
    
    /*
     * EXAMPLE:
     *   
     * rc = strcmp( context_l->xxName, context_r->xxName);
     *
     * if(rc)
     *   return rc;
     *
     * TODO: fix secondary keys (or delete if there are none)
     *
     * if(context_l->yy < context_r->yy) 
     *   return -1;
     *
     * return (context_l->yy == context_r->yy) ? 0 : 1;
     */
}

/************************************************************
 * search tree
 */
/** TODO: set additional indexes as parameters */
monitorPolicyTable_context *
monitorPolicyTable_get( const char *name, int len )
{
    monitorPolicyTable_context tmp;

    /** we should have a secondary index */
    netsnmp_assert(cb.container->next != NULL);
    
    /*
     * TODO: implement compare. Remove this ifdef code and
     * add your own code here.
     */
#ifdef TABLE_CONTAINER_TODO
    snmp_log(LOG_ERR, "monitorPolicyTable_get not implemented!\n" );
    return NULL;
#endif

    /*
     * EXAMPLE:
     *
     * if(len > sizeof(tmp.xxName))
     *   return NULL;
     *
     * strncpy( tmp.xxName, name, sizeof(tmp.xxName) );
     * tmp.xxName_len = len;
     *
     * return CONTAINER_FIND(cb.container->next, &tmp);
     */
}
#endif


/************************************************************
 * Initializes the monitorPolicyTable module
 */
void
init_monitorPolicyTable(void)
{
    initialize_table_monitorPolicyTable();

    /*
     * TODO: perform any startup stuff here
     */

    /* Load monitorPolicyTable from file into memory */
    RBM_Load_Container_From_File ();
}

/************************************************************
 * the *_row_copy routine
 */
static int monitorPolicyTable_row_copy(monitorPolicyTable_context * dst,
                         monitorPolicyTable_context * src)
{
    if(!dst||!src)
        return 1;
        
    /*
     * copy index, if provided
     */
    if(dst->index.oids)
        free(dst->index.oids);
    if(snmp_clone_mem( (void*)&dst->index.oids, src->index.oids,
                           src->index.len * sizeof(oid) )) {
        dst->index.oids = NULL;
        return 1;
    }
    dst->index.len = src->index.len;
    

    /*
     * copy components into the context structure
     */
    dst->monitorPolicyIndex = src->monitorPolicyIndex;

    dst->monitorPolicyID = src->monitorPolicyID;

    memcpy( dst->monitorPolicyName, src->monitorPolicyName, src->monitorPolicyName_len );
    dst->monitorPolicyName_len = src->monitorPolicyName_len;

    memcpy( dst->monitorPolicyDescription, src->monitorPolicyDescription, src->monitorPolicyDescription_len );
    dst->monitorPolicyDescription_len = src->monitorPolicyDescription_len;

    memcpy( dst->monitorPolicyContructor, src->monitorPolicyContructor, src->monitorPolicyContructor_len );
    dst->monitorPolicyContructor_len = src->monitorPolicyContructor_len;

    memcpy( dst->monitorPolicyCreateDate, src->monitorPolicyCreateDate, src->monitorPolicyCreateDate_len );
    dst->monitorPolicyCreateDate_len = src->monitorPolicyCreateDate_len;

    memcpy( dst->monitorPolicyVariable1, src->monitorPolicyVariable1, src->monitorPolicyVariable1_len );
    dst->monitorPolicyVariable1_len = src->monitorPolicyVariable1_len;

    dst->monitorPolicyExpression1 = src->monitorPolicyExpression1;

    dst->monitorPolicyValue1 = src->monitorPolicyValue1;

    dst->monitorPolicyOperation1 = src->monitorPolicyOperation1;

    memcpy( dst->monitorPolicyVariable2, src->monitorPolicyVariable2, src->monitorPolicyVariable2_len );
    dst->monitorPolicyVariable2_len = src->monitorPolicyVariable2_len;

    dst->monitorPolicyExpression2 = src->monitorPolicyExpression2;

    dst->monitorPolicyValue2 = src->monitorPolicyValue2;

    dst->monitorPolicyOperation2 = src->monitorPolicyOperation2;

    memcpy( dst->monitorPolicyVariable3, src->monitorPolicyVariable3, src->monitorPolicyVariable3_len );
    dst->monitorPolicyVariable3_len = src->monitorPolicyVariable3_len;

    dst->monitorPolicyExpression3 = src->monitorPolicyExpression3;

    dst->monitorPolicyValue3 = src->monitorPolicyValue3;

    dst->monitorPolicyOperation3 = src->monitorPolicyOperation3;

    memcpy( dst->monitorPolicyVariable4, src->monitorPolicyVariable4, src->monitorPolicyVariable4_len );
    dst->monitorPolicyVariable4_len = src->monitorPolicyVariable4_len;

    dst->monitorPolicyExpression4 = src->monitorPolicyExpression4;

    dst->monitorPolicyValue4 = src->monitorPolicyValue4;

    dst->monitorPolicyOperation4 = src->monitorPolicyOperation4;

    memcpy( dst->monitorPolicyVariable5, src->monitorPolicyVariable5, src->monitorPolicyVariable5_len );
    dst->monitorPolicyVariable5_len = src->monitorPolicyVariable5_len;

    dst->monitorPolicyExpression5 = src->monitorPolicyExpression5;

    dst->monitorPolicyValue5 = src->monitorPolicyValue5;

    dst->monitorPolicyLevel = src->monitorPolicyLevel;

    dst->monitorPolicySMS = src->monitorPolicySMS;

    dst->monitorPolicyPopup = src->monitorPolicyPopup;

    dst->monitorPolicyEmail = src->monitorPolicyEmail;

    dst->monitorPolicyPSTN = src->monitorPolicyPSTN;

    memcpy( dst->monitorPolicyCommand, src->monitorPolicyCommand, src->monitorPolicyCommand_len );
    dst->monitorPolicyCommand_len = src->monitorPolicyCommand_len;

    memcpy( dst->monitorPolicyScript, src->monitorPolicyScript, src->monitorPolicyScript_len );
    dst->monitorPolicyScript_len = src->monitorPolicyScript_len;

    memcpy( dst->monitorPolicyAlertMessage, src->monitorPolicyAlertMessage, src->monitorPolicyAlertMessage_len );
    dst->monitorPolicyAlertMessage_len = src->monitorPolicyAlertMessage_len;

    dst->monitorPolicyStatus = src->monitorPolicyStatus;

    return 0;
}

#ifdef monitorPolicyTable_SET_HANDLING

/*
 * the *_extract_index routine
 */
int
monitorPolicyTable_extract_index( monitorPolicyTable_context * ctx, netsnmp_index * hdr )
{
    /*
     * temporary local storage for extracting oid index
     */
    netsnmp_variable_list var_monitorPolicyIndex;
    int err;

    /*
     * copy index, if provided
     */
    if(hdr) {
        netsnmp_assert(ctx->index.oids == NULL);
        if(snmp_clone_mem( (void*)&ctx->index.oids, hdr->oids,
                           hdr->len * sizeof(oid) )) {
            return -1;
        }
        ctx->index.len = hdr->len;
    }

    /**
     * Create variable to hold each component of the index
     */
       memset( &var_monitorPolicyIndex, 0x00, sizeof(var_monitorPolicyIndex) );
       var_monitorPolicyIndex.type = ASN_UNSIGNED;
       /** TODO: link this index to the next, or NULL for the last one */
#ifdef TABLE_CONTAINER_TODO
    snmp_log(LOG_ERR, "monitorPolicyTable_extract_index index list not implemented!\n" );
    return 0;
#else
     /* var_monitorPolicyIndex.next_variable = &var_XX; */
#endif


    /*
     * parse the oid into the individual components
     */
    err = parse_oid_indexes( hdr->oids, hdr->len, &var_monitorPolicyIndex );
    if (err == SNMP_ERR_NOERROR) {
       /*
        * copy components into the context structure
        */
                ctx->monitorPolicyIndex = *var_monitorPolicyIndex.val.integer;
   
   
           /*
            * TODO: check index for valid values. For EXAMPLE:
            *
              * if ( *var_monitorPolicyIndex.val.integer != XXX ) {
          *    err = -1;
          * }
          */
    }

    /*
     * parsing may have allocated memory. free it.
     */
    snmp_reset_var_buffers( &var_monitorPolicyIndex );

    return err;
}

/************************************************************
 * the *_can_activate routine is called
 * when a row is changed to determine if all the values
 * set are consistent with the row's rules for a row status
 * of ACTIVE.
 *
 * return 1 if the row could be ACTIVE
 * return 0 if the row is not ready for the ACTIVE state
 */
int monitorPolicyTable_can_activate(monitorPolicyTable_context *undo_ctx,
                      monitorPolicyTable_context *row_ctx,
                      netsnmp_request_group * rg)
{
    /*
     * TODO: check for activation requirements here
     */
    return 1;
}

/************************************************************
 * the *_can_deactivate routine is called when a row that is
 * currently ACTIVE is set to a state other than ACTIVE. If
 * there are conditions in which a row should not be allowed
 * to transition out of the ACTIVE state (such as the row being
 * referred to by another row or table), check for them here.
 *
 * return 1 if the row can be set to a non-ACTIVE state
 * return 0 if the row must remain in the ACTIVE state
 */
int monitorPolicyTable_can_deactivate(monitorPolicyTable_context *undo_ctx,
                        monitorPolicyTable_context *row_ctx,
                        netsnmp_request_group * rg)
{
    /*
     * TODO: check for deactivation requirements here
     */
    return 1;
}

/************************************************************
 * the *_can_delete routine is called to determine if a row
 * can be deleted.
 *
 * return 1 if the row can be deleted
 * return 0 if the row cannot be deleted
 */
int monitorPolicyTable_can_delete(monitorPolicyTable_context *undo_ctx,
                    monitorPolicyTable_context *row_ctx,
                    netsnmp_request_group * rg)
{
    /*
     * probably shouldn't delete a row that we can't
     * deactivate.
     */
    if(monitorPolicyTable_can_deactivate(undo_ctx,row_ctx,rg) != 1)
        return 0;
    
    /*
     * TODO: check for other deletion requirements here
     */
    return 1;
}

#ifdef monitorPolicyTable_ROW_CREATION
/************************************************************
 * the *_create_row routine is called by the table handler
 * to create a new row for a given index. If you need more
 * information (such as column values) to make a decision
 * on creating rows, you must create an initial row here
 * (to hold the column values), and you can examine the
 * situation in more detail in the *_set_reserve1 or later
 * states of set processing. Simple check for a NULL undo_ctx
 * in those states and do detailed creation checking there.
 *
 * returns a newly allocated monitorPolicyTable_context
 *   structure if the specified indexes are not illegal
 * returns NULL for errors or illegal index values.
 */
monitorPolicyTable_context *
monitorPolicyTable_create_row( netsnmp_index* hdr)
{
    monitorPolicyTable_context * ctx =
        SNMP_MALLOC_TYPEDEF(monitorPolicyTable_context);
    if(!ctx)
        return NULL;
        
    /*
     * TODO: check indexes, if necessary.
     */
    if(monitorPolicyTable_extract_index( ctx, hdr )) {
        free(ctx->index.oids);
        free(ctx);
        return NULL;
    }

    /* netsnmp_mutex_init(ctx->lock);
       netsnmp_mutex_lock(ctx->lock); */

    /*
     * TODO: initialize any default values here. This is also
     * first place you really should allocate any memory for
     * yourself to use.  If you allocated memory earlier,
     * make sure you free it for earlier error cases!
     */
    /**
     ctx->monitorPolicyID = 0;
     ctx->monitorPolicyName = 0;
     ctx->monitorPolicyDescription = 0;
     ctx->monitorPolicyContructor = 0;
     ctx->monitorPolicyCreateDate = 0;
     ctx->monitorPolicyVariable1 = 0;
     ctx->monitorPolicyExpression1 = 0;
     ctx->monitorPolicyValue1 = 0;
     ctx->monitorPolicyOperation1 = 0;
     ctx->monitorPolicyVariable2 = 0;
     ctx->monitorPolicyExpression2 = 0;
     ctx->monitorPolicyValue2 = 0;
     ctx->monitorPolicyOperation2 = 0;
     ctx->monitorPolicyVariable3 = 0;
     ctx->monitorPolicyExpression3 = 0;
     ctx->monitorPolicyValue3 = 0;
     ctx->monitorPolicyOperation3 = 0;
     ctx->monitorPolicyVariable4 = 0;
     ctx->monitorPolicyExpression4 = 0;
     ctx->monitorPolicyValue4 = 0;
     ctx->monitorPolicyOperation4 = 0;
     ctx->monitorPolicyVariable5 = 0;
     ctx->monitorPolicyExpression5 = 0;
     ctx->monitorPolicyValue5 = 0;
     ctx->monitorPolicyLevel = 0;
     ctx->monitorPolicySMS = 0;
     ctx->monitorPolicyPopup = 0;
     ctx->monitorPolicyEmail = 0;
     ctx->monitorPolicyPSTN = 0;
     ctx->monitorPolicyCommand = 0;
     ctx->monitorPolicyScript = 0;
     ctx->monitorPolicyAlertMessage = 0;
     ctx->monitorPolicyStatus = 0;
    */

    return ctx;
}
#endif

/************************************************************
 * the *_duplicate row routine
 */
monitorPolicyTable_context *
monitorPolicyTable_duplicate_row( monitorPolicyTable_context * row_ctx)
{
    monitorPolicyTable_context * dup;

    if(!row_ctx)
        return NULL;

    dup = SNMP_MALLOC_TYPEDEF(monitorPolicyTable_context);
    if(!dup)
        return NULL;
        
    if(monitorPolicyTable_row_copy(dup,row_ctx)) {
        free(dup);
        dup = NULL;
    }

    return dup;
}

/************************************************************
 * the *_delete_row method is called to delete a row.
 */
netsnmp_index * monitorPolicyTable_delete_row( monitorPolicyTable_context * ctx )
{
  /* netsnmp_mutex_destroy(ctx->lock); */

    if(ctx->index.oids)
        free(ctx->index.oids);

    /*
     * TODO: release any memory you allocated here...
     */

    /*
     * release header
     */
    free( ctx );

    return NULL;
}


/************************************************************
 * RESERVE is used to check the syntax of all the variables
 * provided, that the values being set are sensible and consistent,
 * and to allocate any resources required for performing the SET.
 * After this stage, the expectation is that the set ought to
 * succeed, though this is not guaranteed. (In fact, with the UCD
 * agent, this is done in two passes - RESERVE1, and
 * RESERVE2, to allow for dependancies between variables).
 *
 * BEFORE calling this routine, the agent will call duplicate_row
 * to create a copy of the row (unless this is a new row; i.e.
 * row_created == 1).
 *
 * next state -> SET_RESERVE2 || SET_FREE
 */
void monitorPolicyTable_set_reserve1( netsnmp_request_group *rg )
{
    monitorPolicyTable_context *row_ctx =
            (monitorPolicyTable_context *)rg->existing_row;
    netsnmp_variable_list *var;
    netsnmp_request_group_item *current;
    int rc;


    /*
     * TODO: loop through columns, check syntax and lengths. For
     * columns which have no dependencies, you could also move
     * the value/range checking here to attempt to catch error
     * cases as early as possible.
     */
    for( current = rg->list; current; current = current->next ) {

        var = current->ri->requestvb;
        rc = SNMP_ERR_NOERROR;

        switch(current->tri->colnum) {

        case COLUMN_MONITORPOLICYID:
            /** UNSIGNED32 = ASN_UNSIGNED */
            rc = netsnmp_check_vb_type_and_size(var, ASN_UNSIGNED,
                                                sizeof(row_ctx->monitorPolicyID));
        break;

        case COLUMN_MONITORPOLICYNAME:
            /** DisplayString = ASN_OCTET_STR */
            rc = netsnmp_check_vb_type_and_size(var, ASN_OCTET_STR,
                                                var->val_len);
        break;

        case COLUMN_MONITORPOLICYDESCRIPTION:
            /** DisplayString = ASN_OCTET_STR */
            rc = netsnmp_check_vb_type_and_size(var, ASN_OCTET_STR,
                                                var->val_len);
        break;

        case COLUMN_MONITORPOLICYCONTRUCTOR:
            /** DisplayString = ASN_OCTET_STR */
            rc = netsnmp_check_vb_type_and_size(var, ASN_OCTET_STR,
                                                var->val_len);
        break;

        case COLUMN_MONITORPOLICYCREATEDATE:
            /** DateAndTime = ASN_OCTET_STR */
            rc = netsnmp_check_vb_type_and_size(var, ASN_OCTET_STR,
                                                var->val_len);
        break;

        case COLUMN_MONITORPOLICYVARIABLE1:
            /** OBJECTID = ASN_OBJECT_ID */
            rc = netsnmp_check_vb_type_and_size(var, ASN_OBJECT_ID,
                                                var->val_len);
        break;

        case COLUMN_MONITORPOLICYEXPRESSION1:
            /** INTEGER = ASN_INTEGER */
            rc = netsnmp_check_vb_type_and_size(var, ASN_INTEGER,
                                                sizeof(row_ctx->monitorPolicyExpression1));
        DEBUGMSGTL(("nuri/rbm", "expr1 value=%d \n", *var->val.integer));

        if ((long) *var->val.integer < RBM_EXPR_MIN ||
            (long) *var->val.integer > RBM_EXPR_MAX)
        {
            DEBUGMSGTL(("nuri/rbm", "1 condn. satisfied\n"));
            rc = SNMP_ERR_WRONGVALUE;
        }

        break;

        case COLUMN_MONITORPOLICYVALUE1:
            /** INTEGER = ASN_INTEGER */
            rc = netsnmp_check_vb_type_and_size(var, ASN_INTEGER,
                                                sizeof(row_ctx->monitorPolicyValue1));
        break;

        case COLUMN_MONITORPOLICYOPERATION1:
            /** INTEGER = ASN_INTEGER */
            rc = netsnmp_check_vb_type_and_size(var, ASN_INTEGER,
                                                sizeof(row_ctx->monitorPolicyOperation1));
        break;

        case COLUMN_MONITORPOLICYVARIABLE2:
            /** OBJECTID = ASN_OBJECT_ID */
            rc = netsnmp_check_vb_type_and_size(var, ASN_OBJECT_ID,
                                                var->val_len);
        break;

        case COLUMN_MONITORPOLICYEXPRESSION2:
            /** INTEGER = ASN_INTEGER */
            rc = netsnmp_check_vb_type_and_size(var, ASN_INTEGER,
                                                sizeof(row_ctx->monitorPolicyExpression2));
        if ((long) *var->val.integer < RBM_EXPR_MIN ||
            (long) *var->val.integer > RBM_EXPR_MAX)
        {
            rc = SNMP_ERR_WRONGVALUE;
        }
        break;

        case COLUMN_MONITORPOLICYVALUE2:
            /** INTEGER = ASN_INTEGER */
            rc = netsnmp_check_vb_type_and_size(var, ASN_INTEGER,
                                                sizeof(row_ctx->monitorPolicyValue2));
        break;

        case COLUMN_MONITORPOLICYOPERATION2:
            /** INTEGER = ASN_INTEGER */
            rc = netsnmp_check_vb_type_and_size(var, ASN_INTEGER,
                                                sizeof(row_ctx->monitorPolicyOperation2));
        break;

        case COLUMN_MONITORPOLICYVARIABLE3:
            /** OBJECTID = ASN_OBJECT_ID */
            rc = netsnmp_check_vb_type_and_size(var, ASN_OBJECT_ID,
                                                var->val_len);
        break;

        case COLUMN_MONITORPOLICYEXPRESSION3:
            /** INTEGER = ASN_INTEGER */
            rc = netsnmp_check_vb_type_and_size(var, ASN_INTEGER,
                                                sizeof(row_ctx->monitorPolicyExpression3));
        if ((long) *var->val.integer < RBM_EXPR_MIN ||
            (long) *var->val.integer > RBM_EXPR_MAX)
        {
            rc = SNMP_ERR_WRONGVALUE;
        }
        break;

        case COLUMN_MONITORPOLICYVALUE3:
            /** INTEGER = ASN_INTEGER */
            rc = netsnmp_check_vb_type_and_size(var, ASN_INTEGER,
                                                sizeof(row_ctx->monitorPolicyValue3));
        break;

        case COLUMN_MONITORPOLICYOPERATION3:
            /** INTEGER = ASN_INTEGER */
            rc = netsnmp_check_vb_type_and_size(var, ASN_INTEGER,
                                                sizeof(row_ctx->monitorPolicyOperation3));
        break;

        case COLUMN_MONITORPOLICYVARIABLE4:
            /** OBJECTID = ASN_OBJECT_ID */
            rc = netsnmp_check_vb_type_and_size(var, ASN_OBJECT_ID,
                                                var->val_len);
        break;

        case COLUMN_MONITORPOLICYEXPRESSION4:
            /** INTEGER = ASN_INTEGER */
            rc = netsnmp_check_vb_type_and_size(var, ASN_INTEGER,
                                                sizeof(row_ctx->monitorPolicyExpression4));
        if ((long) *var->val.integer < RBM_EXPR_MIN ||
            (long) *var->val.integer > RBM_EXPR_MAX)
        {
            rc = SNMP_ERR_WRONGVALUE;
        }
        break;

        case COLUMN_MONITORPOLICYVALUE4:
            /** INTEGER = ASN_INTEGER */
            rc = netsnmp_check_vb_type_and_size(var, ASN_INTEGER,
                                                sizeof(row_ctx->monitorPolicyValue4));
        break;

        case COLUMN_MONITORPOLICYOPERATION4:
            /** INTEGER = ASN_INTEGER */
            rc = netsnmp_check_vb_type_and_size(var, ASN_INTEGER,
                                                sizeof(row_ctx->monitorPolicyOperation4));
        break;

        case COLUMN_MONITORPOLICYVARIABLE5:
            /** OBJECTID = ASN_OBJECT_ID */
            rc = netsnmp_check_vb_type_and_size(var, ASN_OBJECT_ID,
                                                var->val_len);
        break;

        case COLUMN_MONITORPOLICYEXPRESSION5:
            /** INTEGER = ASN_INTEGER */
            rc = netsnmp_check_vb_type_and_size(var, ASN_INTEGER,
                                                sizeof(row_ctx->monitorPolicyExpression5));
        if ((long) *var->val.integer < RBM_EXPR_MIN ||
            (long) *var->val.integer > RBM_EXPR_MAX)
        {
            rc = SNMP_ERR_WRONGVALUE;
        }
        break;

        case COLUMN_MONITORPOLICYVALUE5:
            /** INTEGER = ASN_INTEGER */
            rc = netsnmp_check_vb_type_and_size(var, ASN_INTEGER,
                                                sizeof(row_ctx->monitorPolicyValue5));
        break;

        case COLUMN_MONITORPOLICYLEVEL:
            /** INTEGER = ASN_INTEGER */
            rc = netsnmp_check_vb_type_and_size(var, ASN_INTEGER,
                                                sizeof(row_ctx->monitorPolicyLevel));
        break;

        case COLUMN_MONITORPOLICYSMS:
            /**  =  */
            rc = netsnmp_check_vb_type_and_size(var, ASN_INTEGER,
                                                sizeof(row_ctx->monitorPolicySMS));
        break;

        case COLUMN_MONITORPOLICYPOPUP:
            /**  =  */
            rc = netsnmp_check_vb_type_and_size(var, ASN_INTEGER,
                                                sizeof(row_ctx->monitorPolicyPopup));
        break;

        case COLUMN_MONITORPOLICYEMAIL:
            /**  =  */
            rc = netsnmp_check_vb_type_and_size(var, ASN_INTEGER,
                                                sizeof(row_ctx->monitorPolicyEmail));
        break;

        case COLUMN_MONITORPOLICYPSTN:
            /**  =  */
            rc = netsnmp_check_vb_type_and_size(var, ASN_INTEGER,
                                                sizeof(row_ctx->monitorPolicyPSTN));
        break;

        case COLUMN_MONITORPOLICYCOMMAND:
            /** DisplayString = ASN_OCTET_STR */
            rc = netsnmp_check_vb_type_and_size(var, ASN_OCTET_STR,
                                                var->val_len);
        break;

        case COLUMN_MONITORPOLICYSCRIPT:
            /** DisplayString = ASN_OCTET_STR */
            rc = netsnmp_check_vb_type_and_size(var, ASN_OCTET_STR,
                                                var->val_len);
        break;

        case COLUMN_MONITORPOLICYALERTMESSAGE:
            /** DisplayString = ASN_OCTET_STR */
            rc = netsnmp_check_vb_type_and_size(var, ASN_OCTET_STR,
                                                var->val_len);
        break;

        case COLUMN_MONITORPOLICYSTATUS:
            /** RowStatus = ASN_INTEGER */
            rc = netsnmp_check_vb_type_and_size(var, ASN_INTEGER,
                                                sizeof(row_ctx->monitorPolicyStatus));
        break;

        default: /** We shouldn't get here */
            rc = SNMP_ERR_GENERR;
            snmp_log(LOG_ERR, "unknown column in "
                     "monitorPolicyTable_set_reserve1\n");
        }

        if (rc)
           netsnmp_set_mode_request_error(MODE_SET_BEGIN, current->ri, rc );
        rg->status = SNMP_MAX( rg->status, current->ri->status );
    }

    /*
     * done with all the columns. Could check row related
     * requirements here.
     */
}

void monitorPolicyTable_set_reserve2( netsnmp_request_group *rg )
{
    monitorPolicyTable_context *undo_ctx = (monitorPolicyTable_context *)rg->undo_info;
    netsnmp_request_group_item *current;
    netsnmp_variable_list *var;
    int rc;

    rg->rg_void = rg->list->ri;

    /*
     * TODO: loop through columns, check for valid
     * values and any range constraints.
     */
    for( current = rg->list; current; current = current->next ) {

        var = current->ri->requestvb;
        rc = SNMP_ERR_NOERROR;

        switch(current->tri->colnum) {

        case COLUMN_MONITORPOLICYID:
            /** UNSIGNED32 = ASN_UNSIGNED */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( *var->val.integer != XXX ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYNAME:
            /** DisplayString = ASN_OCTET_STR */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( XXX_check_value( var->val.string, XXX ) ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYDESCRIPTION:
            /** DisplayString = ASN_OCTET_STR */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( XXX_check_value( var->val.string, XXX ) ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYCONTRUCTOR:
            /** DisplayString = ASN_OCTET_STR */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( XXX_check_value( var->val.string, XXX ) ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYCREATEDATE:
            /** DateAndTime = ASN_OCTET_STR */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( XXX_check_value( var->val.string, XXX ) ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYVARIABLE1:
            /** OBJECTID = ASN_OBJECT_ID */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( XXX_check_oid( var ) ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYEXPRESSION1:
            /** INTEGER = ASN_INTEGER */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( *var->val.integer != XXX ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYVALUE1:
            /** INTEGER = ASN_INTEGER */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( *var->val.integer != XXX ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYOPERATION1:
            /** INTEGER = ASN_INTEGER */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( *var->val.integer != XXX ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYVARIABLE2:
            /** OBJECTID = ASN_OBJECT_ID */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( XXX_check_oid( var ) ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYEXPRESSION2:
            /** INTEGER = ASN_INTEGER */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( *var->val.integer != XXX ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYVALUE2:
            /** INTEGER = ASN_INTEGER */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( *var->val.integer != XXX ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYOPERATION2:
            /** INTEGER = ASN_INTEGER */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( *var->val.integer != XXX ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYVARIABLE3:
            /** OBJECTID = ASN_OBJECT_ID */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( XXX_check_oid( var ) ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYEXPRESSION3:
            /** INTEGER = ASN_INTEGER */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( *var->val.integer != XXX ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYVALUE3:
            /** INTEGER = ASN_INTEGER */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( *var->val.integer != XXX ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYOPERATION3:
            /** INTEGER = ASN_INTEGER */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( *var->val.integer != XXX ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYVARIABLE4:
            /** OBJECTID = ASN_OBJECT_ID */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( XXX_check_oid( var ) ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYEXPRESSION4:
            /** INTEGER = ASN_INTEGER */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( *var->val.integer != XXX ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYVALUE4:
            /** INTEGER = ASN_INTEGER */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( *var->val.integer != XXX ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYOPERATION4:
            /** INTEGER = ASN_INTEGER */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( *var->val.integer != XXX ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYVARIABLE5:
            /** OBJECTID = ASN_OBJECT_ID */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( XXX_check_oid( var ) ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYEXPRESSION5:
            /** INTEGER = ASN_INTEGER */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( *var->val.integer != XXX ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYVALUE5:
            /** INTEGER = ASN_INTEGER */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( *var->val.integer != XXX ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYLEVEL:
            /** INTEGER = ASN_INTEGER */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( *var->val.integer != XXX ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYSMS:
            /**  =  */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( *var->val.integer != XXX ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYPOPUP:
            /**  =  */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( *var->val.integer != XXX ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYEMAIL:
            /**  =  */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( *var->val.integer != XXX ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYPSTN:
            /**  =  */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( *var->val.integer != XXX ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYCOMMAND:
            /** DisplayString = ASN_OCTET_STR */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( XXX_check_value( var->val.string, XXX ) ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYSCRIPT:
            /** DisplayString = ASN_OCTET_STR */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( XXX_check_value( var->val.string, XXX ) ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYALERTMESSAGE:
            /** DisplayString = ASN_OCTET_STR */
                    /*
                     * TODO: routine to check valid values
                     *
                     * EXAMPLE:
                     *
                    * if ( XXX_check_value( var->val.string, XXX ) ) {
                *    rc = SNMP_ERR_INCONSISTENTVALUE;
                *    rc = SNMP_ERR_BADVALUE;
                * }
                */
        break;

        case COLUMN_MONITORPOLICYSTATUS:
            /** RowStatus = ASN_INTEGER */
                rc = netsnmp_check_vb_rowstatus(current->ri->requestvb,
                                                undo_ctx ?
                                                undo_ctx->monitorPolicyStatus:0);
                rg->rg_void = current->ri;
        break;

        default: /** We shouldn't get here */
            netsnmp_assert(0); /** why wasn't this caught in reserve1? */
        }

        if (rc)
           netsnmp_set_mode_request_error(MODE_SET_BEGIN, current->ri, rc);
    }

    /*
     * done with all the columns. Could check row related
     * requirements here.
     */
}

/************************************************************
 * Assuming that the RESERVE phases were successful, the next
 * stage is indicated by the action value ACTION. This is used
 * to actually implement the set operation. However, this must
 * either be done into temporary (persistent) storage, or the
 * previous value stored similarly, in case any of the subsequent
 * ACTION calls fail.
 *
 * In your case, changes should be made to row_ctx. A copy of
 * the original row is in undo_ctx.
 */
void monitorPolicyTable_set_action( netsnmp_request_group *rg )
{
    netsnmp_variable_list *var;
    monitorPolicyTable_context *row_ctx = (monitorPolicyTable_context *)rg->existing_row;
    monitorPolicyTable_context *undo_ctx = (monitorPolicyTable_context *)rg->undo_info;
    netsnmp_request_group_item *current;

    int            row_err = 0;

    /*
     * TODO: loop through columns, copy varbind values
     * to context structure for the row.
     */
    for( current = rg->list; current; current = current->next ) {

        var = current->ri->requestvb;

        switch(current->tri->colnum) {

        case COLUMN_MONITORPOLICYID:
            /** UNSIGNED32 = ASN_UNSIGNED */
            row_ctx->monitorPolicyID = *var->val.integer;
        break;

        case COLUMN_MONITORPOLICYNAME:
            /** DisplayString = ASN_OCTET_STR */
            memcpy(row_ctx->monitorPolicyName,var->val.string,var->val_len);
            row_ctx->monitorPolicyName_len = var->val_len;
        break;

        case COLUMN_MONITORPOLICYDESCRIPTION:
            /** DisplayString = ASN_OCTET_STR */
            memcpy(row_ctx->monitorPolicyDescription,var->val.string,var->val_len);
            row_ctx->monitorPolicyDescription_len = var->val_len;
        break;

        case COLUMN_MONITORPOLICYCONTRUCTOR:
            /** DisplayString = ASN_OCTET_STR */
            memcpy(row_ctx->monitorPolicyContructor,var->val.string,var->val_len);
            row_ctx->monitorPolicyContructor_len = var->val_len;
        break;

        case COLUMN_MONITORPOLICYCREATEDATE:
            /** DateAndTime = ASN_OCTET_STR */
            memcpy(row_ctx->monitorPolicyCreateDate,var->val.string,var->val_len);
            row_ctx->monitorPolicyCreateDate_len = var->val_len;
        break;

        case COLUMN_MONITORPOLICYVARIABLE1:
            /** OBJECTID = ASN_OBJECT_ID */
            memcpy(row_ctx->monitorPolicyVariable1,var->val.objid,var->val_len);
            row_ctx->monitorPolicyVariable1_len = var->val_len;
        break;

        case COLUMN_MONITORPOLICYEXPRESSION1:
            /** INTEGER = ASN_INTEGER */
            row_ctx->monitorPolicyExpression1 = *var->val.integer;
        break;

        case COLUMN_MONITORPOLICYVALUE1:
            /** INTEGER = ASN_INTEGER */
            row_ctx->monitorPolicyValue1 = *var->val.integer;
        break;

        case COLUMN_MONITORPOLICYOPERATION1:
            /** INTEGER = ASN_INTEGER */
            row_ctx->monitorPolicyOperation1 = *var->val.integer;
        break;

        case COLUMN_MONITORPOLICYVARIABLE2:
            /** OBJECTID = ASN_OBJECT_ID */
            memcpy(row_ctx->monitorPolicyVariable2,var->val.objid,var->val_len);
            row_ctx->monitorPolicyVariable2_len = var->val_len;
        break;

        case COLUMN_MONITORPOLICYEXPRESSION2:
            /** INTEGER = ASN_INTEGER */
            row_ctx->monitorPolicyExpression2 = *var->val.integer;
        break;

        case COLUMN_MONITORPOLICYVALUE2:
            /** INTEGER = ASN_INTEGER */
            row_ctx->monitorPolicyValue2 = *var->val.integer;
        break;

        case COLUMN_MONITORPOLICYOPERATION2:
            /** INTEGER = ASN_INTEGER */
            row_ctx->monitorPolicyOperation2 = *var->val.integer;
        break;

        case COLUMN_MONITORPOLICYVARIABLE3:
            /** OBJECTID = ASN_OBJECT_ID */
            memcpy(row_ctx->monitorPolicyVariable3,var->val.objid,var->val_len);
            row_ctx->monitorPolicyVariable3_len = var->val_len;
        break;

        case COLUMN_MONITORPOLICYEXPRESSION3:
            /** INTEGER = ASN_INTEGER */
            row_ctx->monitorPolicyExpression3 = *var->val.integer;
        break;

        case COLUMN_MONITORPOLICYVALUE3:
            /** INTEGER = ASN_INTEGER */
            row_ctx->monitorPolicyValue3 = *var->val.integer;
        break;

        case COLUMN_MONITORPOLICYOPERATION3:
            /** INTEGER = ASN_INTEGER */
            row_ctx->monitorPolicyOperation3 = *var->val.integer;
        break;

        case COLUMN_MONITORPOLICYVARIABLE4:
            /** OBJECTID = ASN_OBJECT_ID */
            memcpy(row_ctx->monitorPolicyVariable4,var->val.objid,var->val_len);
            row_ctx->monitorPolicyVariable4_len = var->val_len;
        break;

        case COLUMN_MONITORPOLICYEXPRESSION4:
            /** INTEGER = ASN_INTEGER */
            row_ctx->monitorPolicyExpression4 = *var->val.integer;
        break;

        case COLUMN_MONITORPOLICYVALUE4:
            /** INTEGER = ASN_INTEGER */
            row_ctx->monitorPolicyValue4 = *var->val.integer;
        break;

        case COLUMN_MONITORPOLICYOPERATION4:
            /** INTEGER = ASN_INTEGER */
            row_ctx->monitorPolicyOperation4 = *var->val.integer;
        break;

        case COLUMN_MONITORPOLICYVARIABLE5:
            /** OBJECTID = ASN_OBJECT_ID */
            memcpy(row_ctx->monitorPolicyVariable5,var->val.objid,var->val_len);
            row_ctx->monitorPolicyVariable5_len = var->val_len;
        break;

        case COLUMN_MONITORPOLICYEXPRESSION5:
            /** INTEGER = ASN_INTEGER */
            row_ctx->monitorPolicyExpression5 = *var->val.integer;
        break;

        case COLUMN_MONITORPOLICYVALUE5:
            /** INTEGER = ASN_INTEGER */
            row_ctx->monitorPolicyValue5 = *var->val.integer;
        break;

        case COLUMN_MONITORPOLICYLEVEL:
            /** INTEGER = ASN_INTEGER */
            row_ctx->monitorPolicyLevel = *var->val.integer;
        break;

        case COLUMN_MONITORPOLICYSMS:
            /**  =  */
            row_ctx->monitorPolicySMS = *var->val.integer;
        break;

        case COLUMN_MONITORPOLICYPOPUP:
            /**  =  */
            row_ctx->monitorPolicyPopup = *var->val.integer;
        break;

        case COLUMN_MONITORPOLICYEMAIL:
            /**  =  */
            row_ctx->monitorPolicyEmail = *var->val.integer;
        break;

        case COLUMN_MONITORPOLICYPSTN:
            /**  =  */
            row_ctx->monitorPolicyPSTN = *var->val.integer;
        break;

        case COLUMN_MONITORPOLICYCOMMAND:
            /** DisplayString = ASN_OCTET_STR */
            memcpy(row_ctx->monitorPolicyCommand,var->val.string,var->val_len);
            row_ctx->monitorPolicyCommand_len = var->val_len;
        break;

        case COLUMN_MONITORPOLICYSCRIPT:
            /** DisplayString = ASN_OCTET_STR */
            memcpy(row_ctx->monitorPolicyScript,var->val.string,var->val_len);
            row_ctx->monitorPolicyScript_len = var->val_len;
        break;

        case COLUMN_MONITORPOLICYALERTMESSAGE:
            /** DisplayString = ASN_OCTET_STR */
            memcpy(row_ctx->monitorPolicyAlertMessage,var->val.string,var->val_len);
            row_ctx->monitorPolicyAlertMessage_len = var->val_len;
        break;

        case COLUMN_MONITORPOLICYSTATUS:
            /** RowStatus = ASN_INTEGER */
            row_ctx->monitorPolicyStatus = *var->val.integer;
        break;

        default: /** We shouldn't get here */
            netsnmp_assert(0); /** why wasn't this caught in reserve1? */
        }
    }

    /*
     * done with all the columns. Could check row related
     * requirements here.
     */
#ifndef monitorPolicyTable_CAN_MODIFY_ACTIVE_ROW
    if( undo_ctx && RS_IS_ACTIVE(undo_ctx->monitorPolicyStatus) &&
        row_ctx && RS_IS_ACTIVE(row_ctx->monitorPolicyStatus) ) {
            row_err = 1;
    }
#endif

    /*
     * check activation/deactivation
     */
    row_err = netsnmp_table_array_check_row_status(&cb, rg,
                                  row_ctx ? &row_ctx->monitorPolicyStatus : NULL,
                                  undo_ctx ? &undo_ctx->monitorPolicyStatus : NULL);
    if(row_err) {
        netsnmp_set_mode_request_error(MODE_SET_BEGIN,
                                       (netsnmp_request_info*)rg->rg_void,
                                       row_err);
        return;
    }

    /*
     * TODO: if you have dependencies on other tables, this would be
     * a good place to check those, too.
     */
}

/************************************************************
 * Only once the ACTION phase has completed successfully, can
 * the final COMMIT phase be run. This is used to complete any
 * writes that were done into temporary storage, and then release
 * any allocated resources. Note that all the code in this phase
 * should be "safe" code that cannot possibly fail (cue
 * hysterical laughter). The whole intent of the ACTION/COMMIT
 * division is that all of the fallible code should be done in
 * the ACTION phase, so that it can be backed out if necessary.
 *
 * BEFORE calling this routine, the agent will update the
 * container (inserting a row if row_created == 1, or removing
 * the row if row_deleted == 1).
 *
 * AFTER calling this routine, the agent will delete the
 * undo_info.
 */
void monitorPolicyTable_set_commit( netsnmp_request_group *rg )
{
    FILE *fp = NULL;
    netsnmp_variable_list *var;
    monitorPolicyTable_context *ctx = NULL;
    netsnmp_request_group_item *current;

    fp = fopen (RBM_CONTAINER_FILE, "wb");

    if (fp == (FILE *) NULL)
    {
        DEBUGMSGTL(("monitorPolicyTable_set_commit",
                    "Couldn't open RBM_CONTAINER_FILE error no: %d\n", errno));
        return;
    }

    ctx = CONTAINER_FIRST(cb.container);
    if (ctx)
    {
        /* Assign ctx index->oids to idx */
        ctx->idx = (unsigned long) *ctx->index.oids;

       if (fwrite (ctx, sizeof (monitorPolicyTable_context), 1, fp) != 1)
       {
           DEBUGMSGTL(("monitorPolicyTable_set_commit",
                       "1 write ctx error errno: %d\n", errno));
           return;
       }
    }

    while (ctx)
    {
        ctx = CONTAINER_NEXT(cb.container,ctx);
        if(ctx)
        {
            /* Assign ctx index->oids to idx */
            ctx->idx = (unsigned long) *ctx->index.oids;

            if (fwrite (ctx, sizeof (monitorPolicyTable_context), 1, fp) != 1)
            {
               DEBUGMSGTL(("monitorPolicyTable_set_commit",
                           "2 write ctx error errno: %d\n", errno));
               return;
            }
        }
    }

    fclose (fp);


    /*
     * loop through columns
     */
    for( current = rg->list; current; current = current->next ) {

        var = current->ri->requestvb;

        switch(current->tri->colnum) {

        case COLUMN_MONITORPOLICYID:
            /** UNSIGNED32 = ASN_UNSIGNED */
        break;

        case COLUMN_MONITORPOLICYNAME:
            /** DisplayString = ASN_OCTET_STR */
        break;

        case COLUMN_MONITORPOLICYDESCRIPTION:
            /** DisplayString = ASN_OCTET_STR */
        break;

        case COLUMN_MONITORPOLICYCONTRUCTOR:
            /** DisplayString = ASN_OCTET_STR */
        break;

        case COLUMN_MONITORPOLICYCREATEDATE:
            /** DateAndTime = ASN_OCTET_STR */
        break;

        case COLUMN_MONITORPOLICYVARIABLE1:
            /** OBJECTID = ASN_OBJECT_ID */
        break;

        case COLUMN_MONITORPOLICYEXPRESSION1:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYVALUE1:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYOPERATION1:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYVARIABLE2:
            /** OBJECTID = ASN_OBJECT_ID */
        break;

        case COLUMN_MONITORPOLICYEXPRESSION2:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYVALUE2:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYOPERATION2:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYVARIABLE3:
            /** OBJECTID = ASN_OBJECT_ID */
        break;

        case COLUMN_MONITORPOLICYEXPRESSION3:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYVALUE3:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYOPERATION3:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYVARIABLE4:
            /** OBJECTID = ASN_OBJECT_ID */
        break;

        case COLUMN_MONITORPOLICYEXPRESSION4:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYVALUE4:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYOPERATION4:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYVARIABLE5:
            /** OBJECTID = ASN_OBJECT_ID */
        break;

        case COLUMN_MONITORPOLICYEXPRESSION5:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYVALUE5:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYLEVEL:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYSMS:
            /**  =  */
        break;

        case COLUMN_MONITORPOLICYPOPUP:
            /**  =  */
        break;

        case COLUMN_MONITORPOLICYEMAIL:
            /**  =  */
        break;

        case COLUMN_MONITORPOLICYPSTN:
            /**  =  */
        break;

        case COLUMN_MONITORPOLICYCOMMAND:
            /** DisplayString = ASN_OCTET_STR */
        break;

        case COLUMN_MONITORPOLICYSCRIPT:
            /** DisplayString = ASN_OCTET_STR */
        break;

        case COLUMN_MONITORPOLICYALERTMESSAGE:
            /** DisplayString = ASN_OCTET_STR */
        break;

        case COLUMN_MONITORPOLICYSTATUS:
            /** RowStatus = ASN_INTEGER */
        break;

        default: /** We shouldn't get here */
            netsnmp_assert(0); /** why wasn't this caught in reserve1? */
        }
    }

    /*
     * done with all the columns. Could check row related
     * requirements here.
     */
}

/************************************************************
 * If either of the RESERVE calls fail, the write routines
 * are called again with the FREE action, to release any resources
 * that have been allocated. The agent will then return a failure
 * response to the requesting application.
 *
 * AFTER calling this routine, the agent will delete undo_info.
 */
void monitorPolicyTable_set_free( netsnmp_request_group *rg )
{
    netsnmp_variable_list *var;
    netsnmp_request_group_item *current;

    /*
     * loop through columns
     */
    for( current = rg->list; current; current = current->next ) {

        var = current->ri->requestvb;

        switch(current->tri->colnum) {

        case COLUMN_MONITORPOLICYID:
            /** UNSIGNED32 = ASN_UNSIGNED */
        break;

        case COLUMN_MONITORPOLICYNAME:
            /** DisplayString = ASN_OCTET_STR */
        break;

        case COLUMN_MONITORPOLICYDESCRIPTION:
            /** DisplayString = ASN_OCTET_STR */
        break;

        case COLUMN_MONITORPOLICYCONTRUCTOR:
            /** DisplayString = ASN_OCTET_STR */
        break;

        case COLUMN_MONITORPOLICYCREATEDATE:
            /** DateAndTime = ASN_OCTET_STR */
        break;

        case COLUMN_MONITORPOLICYVARIABLE1:
            /** OBJECTID = ASN_OBJECT_ID */
        break;

        case COLUMN_MONITORPOLICYEXPRESSION1:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYVALUE1:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYOPERATION1:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYVARIABLE2:
            /** OBJECTID = ASN_OBJECT_ID */
        break;

        case COLUMN_MONITORPOLICYEXPRESSION2:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYVALUE2:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYOPERATION2:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYVARIABLE3:
            /** OBJECTID = ASN_OBJECT_ID */
        break;

        case COLUMN_MONITORPOLICYEXPRESSION3:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYVALUE3:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYOPERATION3:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYVARIABLE4:
            /** OBJECTID = ASN_OBJECT_ID */
        break;

        case COLUMN_MONITORPOLICYEXPRESSION4:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYVALUE4:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYOPERATION4:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYVARIABLE5:
            /** OBJECTID = ASN_OBJECT_ID */
        break;

        case COLUMN_MONITORPOLICYEXPRESSION5:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYVALUE5:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYLEVEL:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYSMS:
            /**  =  */
        break;

        case COLUMN_MONITORPOLICYPOPUP:
            /**  =  */
        break;

        case COLUMN_MONITORPOLICYEMAIL:
            /**  =  */
        break;

        case COLUMN_MONITORPOLICYPSTN:
            /**  =  */
        break;

        case COLUMN_MONITORPOLICYCOMMAND:
            /** DisplayString = ASN_OCTET_STR */
        break;

        case COLUMN_MONITORPOLICYSCRIPT:
            /** DisplayString = ASN_OCTET_STR */
        break;

        case COLUMN_MONITORPOLICYALERTMESSAGE:
            /** DisplayString = ASN_OCTET_STR */
        break;

        case COLUMN_MONITORPOLICYSTATUS:
            /** RowStatus = ASN_INTEGER */
        break;

        default: /** We shouldn't get here */
            /** should have been logged in reserve1 */
	  break;
        }
    }

    /*
     * done with all the columns. Could check row related
     * requirements here.
     */
}

/************************************************************
 * If the ACTION phase does fail (for example due to an apparently
 * valid, but unacceptable value, or an unforeseen problem), then
 * the list of write routines are called again, with the UNDO
 * action. This requires the routine to reset the value that was
 * changed to its previous value (assuming it was actually changed),
 * and then to release any resources that had been allocated. As
 * with the FREE phase, the agent will then return an indication
 * of the error to the requesting application.
 *
 * BEFORE calling this routine, the agent will update the container
 * (remove any newly inserted row, re-insert any removed row).
 *
 * AFTER calling this routing, the agent will call row_copy
 * to restore the data in existing_row from the date in undo_info.
 * Then undo_info will be deleted (or existing row, if row_created
 * == 1).
 */
void monitorPolicyTable_set_undo( netsnmp_request_group *rg )
{
    netsnmp_variable_list *var;
    netsnmp_request_group_item *current;

    /*
     * loop through columns
     */
    for( current = rg->list; current; current = current->next ) {

        var = current->ri->requestvb;

        switch(current->tri->colnum) {

        case COLUMN_MONITORPOLICYID:
            /** UNSIGNED32 = ASN_UNSIGNED */
        break;

        case COLUMN_MONITORPOLICYNAME:
            /** DisplayString = ASN_OCTET_STR */
        break;

        case COLUMN_MONITORPOLICYDESCRIPTION:
            /** DisplayString = ASN_OCTET_STR */
        break;

        case COLUMN_MONITORPOLICYCONTRUCTOR:
            /** DisplayString = ASN_OCTET_STR */
        break;

        case COLUMN_MONITORPOLICYCREATEDATE:
            /** DateAndTime = ASN_OCTET_STR */
        break;

        case COLUMN_MONITORPOLICYVARIABLE1:
            /** OBJECTID = ASN_OBJECT_ID */
        break;

        case COLUMN_MONITORPOLICYEXPRESSION1:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYVALUE1:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYOPERATION1:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYVARIABLE2:
            /** OBJECTID = ASN_OBJECT_ID */
        break;

        case COLUMN_MONITORPOLICYEXPRESSION2:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYVALUE2:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYOPERATION2:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYVARIABLE3:
            /** OBJECTID = ASN_OBJECT_ID */
        break;

        case COLUMN_MONITORPOLICYEXPRESSION3:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYVALUE3:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYOPERATION3:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYVARIABLE4:
            /** OBJECTID = ASN_OBJECT_ID */
        break;

        case COLUMN_MONITORPOLICYEXPRESSION4:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYVALUE4:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYOPERATION4:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYVARIABLE5:
            /** OBJECTID = ASN_OBJECT_ID */
        break;

        case COLUMN_MONITORPOLICYEXPRESSION5:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYVALUE5:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYLEVEL:
            /** INTEGER = ASN_INTEGER */
        break;

        case COLUMN_MONITORPOLICYSMS:
            /**  =  */
        break;

        case COLUMN_MONITORPOLICYPOPUP:
            /**  =  */
        break;

        case COLUMN_MONITORPOLICYEMAIL:
            /**  =  */
        break;

        case COLUMN_MONITORPOLICYPSTN:
            /**  =  */
        break;

        case COLUMN_MONITORPOLICYCOMMAND:
            /** DisplayString = ASN_OCTET_STR */
        break;

        case COLUMN_MONITORPOLICYSCRIPT:
            /** DisplayString = ASN_OCTET_STR */
        break;

        case COLUMN_MONITORPOLICYALERTMESSAGE:
            /** DisplayString = ASN_OCTET_STR */
        break;

        case COLUMN_MONITORPOLICYSTATUS:
            /** RowStatus = ASN_INTEGER */
        break;

        default: /** We shouldn't get here */
            netsnmp_assert(0); /** why wasn't this caught in reserve1? */
        }
    }

    /*
     * done with all the columns. Could check row related
     * requirements here.
     */
}

#endif /** monitorPolicyTable_SET_HANDLING */


/************************************************************
 *
 * Initialize the monitorPolicyTable table by defining its contents and how it's structured
 */
void
initialize_table_monitorPolicyTable(void)
{
    netsnmp_table_registration_info *table_info;

    if(my_handler) {
        snmp_log(LOG_ERR, "initialize_table_monitorPolicyTable_handler called again\n");
        return;
    }

    memset(&cb, 0x00, sizeof(cb));

    /** create the table structure itself */
    table_info = SNMP_MALLOC_TYPEDEF(netsnmp_table_registration_info);

    /* if your table is read only, it's easiest to change the
       HANDLER_CAN_RWRITE definition below to HANDLER_CAN_RONLY */
    my_handler = netsnmp_create_handler_registration("monitorPolicyTable",
                                             netsnmp_table_array_helper_handler,
                                             monitorPolicyTable_oid,
                                             monitorPolicyTable_oid_len,
                                             HANDLER_CAN_RWRITE);
            
    if (!my_handler || !table_info) {
        snmp_log(LOG_ERR, "malloc failed in "
                 "initialize_table_monitorPolicyTable_handler\n");
        return; /** mallocs failed */
    }

    /***************************************************
     * Setting up the table's definition
     */
    /*
     * TODO: add any external indexes here.
     */

    /*
     * internal indexes
     */
        /** index: monitorPolicyIndex */
        netsnmp_table_helper_add_index(table_info, ASN_UNSIGNED);

    table_info->min_column = monitorPolicyTable_COL_MIN;
    table_info->max_column = monitorPolicyTable_COL_MAX;

    /***************************************************
     * registering the table with the master agent
     */
    cb.get_value = monitorPolicyTable_get_value;
    cb.container = netsnmp_container_find("monitorPolicyTable_primary:"
                                          "monitorPolicyTable:"
                                          "table_container");
#ifdef monitorPolicyTable_IDX2
    netsnmp_container_add_index(cb.container,
                                netsnmp_container_find("monitorPolicyTable_secondary:"
                                                       "monitorPolicyTable:"
                                                       "table_container"));
    cb.container->next->compare = monitorPolicyTable_cmp;
#endif
#ifdef monitorPolicyTable_SET_HANDLING
    cb.can_set = 1;
#ifdef monitorPolicyTable_ROW_CREATION
    cb.create_row = (UserRowMethod*)monitorPolicyTable_create_row;
#endif
    cb.duplicate_row = (UserRowMethod*)monitorPolicyTable_duplicate_row;
    cb.delete_row = (UserRowMethod*)monitorPolicyTable_delete_row;
    cb.row_copy = (Netsnmp_User_Row_Operation *)monitorPolicyTable_row_copy;

    cb.can_activate = (Netsnmp_User_Row_Action *)monitorPolicyTable_can_activate;
    cb.can_deactivate = (Netsnmp_User_Row_Action *)monitorPolicyTable_can_deactivate;
    cb.can_delete = (Netsnmp_User_Row_Action *)monitorPolicyTable_can_delete;

    cb.set_reserve1 = monitorPolicyTable_set_reserve1;
    cb.set_reserve2 = monitorPolicyTable_set_reserve2;
    cb.set_action = monitorPolicyTable_set_action;
    cb.set_commit = monitorPolicyTable_set_commit;
    cb.set_free = monitorPolicyTable_set_free;
    cb.set_undo = monitorPolicyTable_set_undo;
#endif
    DEBUGMSGTL(("initialize_table_monitorPolicyTable",
                "Registering table monitorPolicyTable "
                "as a table array\n"));
    netsnmp_table_container_register(my_handler, table_info, &cb,
                                     cb.container, 1);
}

/************************************************************
 * monitorPolicyTable_get_value
 */
int monitorPolicyTable_get_value(
            netsnmp_request_info *request,
            netsnmp_index *item,
            netsnmp_table_request_info *table_info )
{
    netsnmp_variable_list *var = request->requestvb;
    monitorPolicyTable_context *context = (monitorPolicyTable_context *)item;

    switch(table_info->colnum) {

        case COLUMN_MONITORPOLICYINDEX:
            /** UNSIGNED32 = ASN_UNSIGNED */
            snmp_set_var_typed_value(var, ASN_UNSIGNED,
                         (char*)&context->monitorPolicyIndex,
                         sizeof(context->monitorPolicyIndex) );
        break;
    
        case COLUMN_MONITORPOLICYID:
            /** UNSIGNED32 = ASN_UNSIGNED */
            snmp_set_var_typed_value(var, ASN_UNSIGNED,
                         (char*)&context->monitorPolicyID,
                         sizeof(context->monitorPolicyID) );
        break;
    
        case COLUMN_MONITORPOLICYNAME:
            /** DisplayString = ASN_OCTET_STR */
            snmp_set_var_typed_value(var, ASN_OCTET_STR,
                         (char*)&context->monitorPolicyName,
                         context->monitorPolicyName_len );
        break;
    
        case COLUMN_MONITORPOLICYDESCRIPTION:
            /** DisplayString = ASN_OCTET_STR */
            snmp_set_var_typed_value(var, ASN_OCTET_STR,
                         (char*)&context->monitorPolicyDescription,
                         context->monitorPolicyDescription_len );
        break;
    
        case COLUMN_MONITORPOLICYCONTRUCTOR:
            /** DisplayString = ASN_OCTET_STR */
            snmp_set_var_typed_value(var, ASN_OCTET_STR,
                         (char*)&context->monitorPolicyContructor,
                         context->monitorPolicyContructor_len );
        break;
    
        case COLUMN_MONITORPOLICYCREATEDATE:
            /** DateAndTime = ASN_OCTET_STR */
            snmp_set_var_typed_value(var, ASN_OCTET_STR,
                         (char*)&context->monitorPolicyCreateDate,
                         context->monitorPolicyCreateDate_len );
        break;
    
        case COLUMN_MONITORPOLICYVARIABLE1:
            /** OBJECTID = ASN_OBJECT_ID */
            snmp_set_var_typed_value(var, ASN_OBJECT_ID,
                         (char*)&context->monitorPolicyVariable1,
                         context->monitorPolicyVariable1_len );
        break;
    
        case COLUMN_MONITORPOLICYEXPRESSION1:
            /** INTEGER = ASN_INTEGER */
            snmp_set_var_typed_value(var, ASN_INTEGER,
                         (char*)&context->monitorPolicyExpression1,
                         sizeof(context->monitorPolicyExpression1) );
        break;
    
        case COLUMN_MONITORPOLICYVALUE1:
            /** INTEGER = ASN_INTEGER */
            snmp_set_var_typed_value(var, ASN_INTEGER,
                         (char*)&context->monitorPolicyValue1,
                         sizeof(context->monitorPolicyValue1) );
        break;
    
        case COLUMN_MONITORPOLICYOPERATION1:
            /** INTEGER = ASN_INTEGER */
            snmp_set_var_typed_value(var, ASN_INTEGER,
                         (char*)&context->monitorPolicyOperation1,
                         sizeof(context->monitorPolicyOperation1) );
        break;
    
        case COLUMN_MONITORPOLICYVARIABLE2:
            /** OBJECTID = ASN_OBJECT_ID */
            snmp_set_var_typed_value(var, ASN_OBJECT_ID,
                         (char*)&context->monitorPolicyVariable2,
                         context->monitorPolicyVariable2_len );
        break;
    
        case COLUMN_MONITORPOLICYEXPRESSION2:
            /** INTEGER = ASN_INTEGER */
            snmp_set_var_typed_value(var, ASN_INTEGER,
                         (char*)&context->monitorPolicyExpression2,
                         sizeof(context->monitorPolicyExpression2) );
        break;
    
        case COLUMN_MONITORPOLICYVALUE2:
            /** INTEGER = ASN_INTEGER */
            snmp_set_var_typed_value(var, ASN_INTEGER,
                         (char*)&context->monitorPolicyValue2,
                         sizeof(context->monitorPolicyValue2) );
        break;
    
        case COLUMN_MONITORPOLICYOPERATION2:
            /** INTEGER = ASN_INTEGER */
            snmp_set_var_typed_value(var, ASN_INTEGER,
                         (char*)&context->monitorPolicyOperation2,
                         sizeof(context->monitorPolicyOperation2) );
        break;
    
        case COLUMN_MONITORPOLICYVARIABLE3:
            /** OBJECTID = ASN_OBJECT_ID */
            snmp_set_var_typed_value(var, ASN_OBJECT_ID,
                         (char*)&context->monitorPolicyVariable3,
                         context->monitorPolicyVariable3_len );
        break;
    
        case COLUMN_MONITORPOLICYEXPRESSION3:
            /** INTEGER = ASN_INTEGER */
            snmp_set_var_typed_value(var, ASN_INTEGER,
                         (char*)&context->monitorPolicyExpression3,
                         sizeof(context->monitorPolicyExpression3) );
        break;
    
        case COLUMN_MONITORPOLICYVALUE3:
            /** INTEGER = ASN_INTEGER */
            snmp_set_var_typed_value(var, ASN_INTEGER,
                         (char*)&context->monitorPolicyValue3,
                         sizeof(context->monitorPolicyValue3) );
        break;
    
        case COLUMN_MONITORPOLICYOPERATION3:
            /** INTEGER = ASN_INTEGER */
            snmp_set_var_typed_value(var, ASN_INTEGER,
                         (char*)&context->monitorPolicyOperation3,
                         sizeof(context->monitorPolicyOperation3) );
        break;
    
        case COLUMN_MONITORPOLICYVARIABLE4:
            /** OBJECTID = ASN_OBJECT_ID */
            snmp_set_var_typed_value(var, ASN_OBJECT_ID,
                         (char*)&context->monitorPolicyVariable4,
                         context->monitorPolicyVariable4_len );
        break;
    
        case COLUMN_MONITORPOLICYEXPRESSION4:
            /** INTEGER = ASN_INTEGER */
            snmp_set_var_typed_value(var, ASN_INTEGER,
                         (char*)&context->monitorPolicyExpression4,
                         sizeof(context->monitorPolicyExpression4) );
        break;
    
        case COLUMN_MONITORPOLICYVALUE4:
            /** INTEGER = ASN_INTEGER */
            snmp_set_var_typed_value(var, ASN_INTEGER,
                         (char*)&context->monitorPolicyValue4,
                         sizeof(context->monitorPolicyValue4) );
        break;
    
        case COLUMN_MONITORPOLICYOPERATION4:
            /** INTEGER = ASN_INTEGER */
            snmp_set_var_typed_value(var, ASN_INTEGER,
                         (char*)&context->monitorPolicyOperation4,
                         sizeof(context->monitorPolicyOperation4) );
        break;
    
        case COLUMN_MONITORPOLICYVARIABLE5:
            /** OBJECTID = ASN_OBJECT_ID */
            snmp_set_var_typed_value(var, ASN_OBJECT_ID,
                         (char*)&context->monitorPolicyVariable5,
                         context->monitorPolicyVariable5_len );
        break;
    
        case COLUMN_MONITORPOLICYEXPRESSION5:
            /** INTEGER = ASN_INTEGER */
            snmp_set_var_typed_value(var, ASN_INTEGER,
                         (char*)&context->monitorPolicyExpression5,
                         sizeof(context->monitorPolicyExpression5) );
        break;
    
        case COLUMN_MONITORPOLICYVALUE5:
            /** INTEGER = ASN_INTEGER */
            snmp_set_var_typed_value(var, ASN_INTEGER,
                         (char*)&context->monitorPolicyValue5,
                         sizeof(context->monitorPolicyValue5) );
        break;
    
        case COLUMN_MONITORPOLICYLEVEL:
            /** INTEGER = ASN_INTEGER */
            snmp_set_var_typed_value(var, ASN_INTEGER,
                         (char*)&context->monitorPolicyLevel,
                         sizeof(context->monitorPolicyLevel) );
        break;
    
        case COLUMN_MONITORPOLICYSMS:
            /**  =  */
            snmp_set_var_typed_value(var, ASN_INTEGER,
                         (char*)&context->monitorPolicySMS,
                         sizeof(context->monitorPolicySMS) );
        break;
    
        case COLUMN_MONITORPOLICYPOPUP:
            /**  =  */
            snmp_set_var_typed_value(var, ASN_INTEGER,
                         (char*)&context->monitorPolicyPopup,
                         sizeof(context->monitorPolicyPopup) );
        break;
    
        case COLUMN_MONITORPOLICYEMAIL:
            /**  =  */
            snmp_set_var_typed_value(var, ASN_INTEGER,
                         (char*)&context->monitorPolicyEmail,
                         sizeof(context->monitorPolicyEmail) );
        break;
    
        case COLUMN_MONITORPOLICYPSTN:
            /**  =  */
            snmp_set_var_typed_value(var, ASN_INTEGER,
                         (char*)&context->monitorPolicyPSTN,
                         sizeof(context->monitorPolicyPSTN) );
        break;
    
        case COLUMN_MONITORPOLICYCOMMAND:
            /** DisplayString = ASN_OCTET_STR */
            snmp_set_var_typed_value(var, ASN_OCTET_STR,
                         (char*)&context->monitorPolicyCommand,
                         context->monitorPolicyCommand_len );
        break;
    
        case COLUMN_MONITORPOLICYSCRIPT:
            /** DisplayString = ASN_OCTET_STR */
            snmp_set_var_typed_value(var, ASN_OCTET_STR,
                         (char*)&context->monitorPolicyScript,
                         context->monitorPolicyScript_len );
        break;
    
        case COLUMN_MONITORPOLICYALERTMESSAGE:
            /** DisplayString = ASN_OCTET_STR */
            snmp_set_var_typed_value(var, ASN_OCTET_STR,
                         (char*)&context->monitorPolicyAlertMessage,
                         context->monitorPolicyAlertMessage_len );
        break;
    
        case COLUMN_MONITORPOLICYSTATUS:
            /** RowStatus = ASN_INTEGER */
            snmp_set_var_typed_value(var, ASN_INTEGER,
                         (char*)&context->monitorPolicyStatus,
                         sizeof(context->monitorPolicyStatus) );
        break;
    
    default: /** We shouldn't get here */
        snmp_log(LOG_ERR, "unknown column in "
                 "monitorPolicyTable_get_value\n");
        return SNMP_ERR_GENERR;
    }
    return SNMP_ERR_NOERROR;
}

/************************************************************
 * monitorPolicyTable_get_by_idx
 */
const monitorPolicyTable_context *
monitorPolicyTable_get_by_idx(netsnmp_index * hdr)
{
    return (const monitorPolicyTable_context *)
        CONTAINER_FIND(cb.container, hdr );
}

/*****************************************************************************
 * name             :   RBM_Load_Container_From_File
 * description      :   Loads the monitorPolicyTable from file to memory context
 * input parameters :
 * output parameters:
 * return type      :   void
 * global variables :
 * calls            :   init_monitorPolicyTable
 *****************************************************************************/

void
RBM_Load_Container_From_File ()
{
    FILE *fp = NULL;
    int iCnt=0;
    monitorPolicyTable_context stReadCtx;
    monitorPolicyTable_context *pstContext = NULL;

    /* Open the container file and load all monitorPolicyTable rows from
       this file during start up. */

    fp = fopen (RBM_CONTAINER_FILE, "rb");
    if (fp == (FILE *) NULL)
    {
      DEBUGMSGTL(("RBM_Load_Container_From_File",
                  "RBM_CONTAINER_FILE file open failed errno=%d\n", errno));
    }
    else
    {
	/* Loading the monitorPolicyTable rows from file to sub-agent memory */
        while (fread (&stReadCtx, sizeof (stReadCtx), 1, fp) == 1)
        {
            pstContext = SNMP_MALLOC_TYPEDEF (monitorPolicyTable_context);
            memcpy (pstContext,&stReadCtx, sizeof (monitorPolicyTable_context));

            netsnmp_assert(pstContext->index.oids == NULL);
            if(snmp_clone_mem( (void*)&pstContext->index.oids, &stReadCtx.idx,
                             stReadCtx.index.len * sizeof(oid) )) {
                DEBUGMSGTL(("RBM_Load_Container_From_File",
                            "snmp_clone_mem failed.\n"));
                return;
            }

            pstContext->index.len = stReadCtx.index.len;

            iCnt = CONTAINER_INSERT (cb.container, pstContext);
            if (iCnt)
            {
                DEBUGMSGTL(("RBM_Load_Container_From_File",
                            "ERROR IN CONTAINER_INSERT FUNCION\n"));
            }
       }

        DEBUGMSGTL(("RBM_Load_Container_From_File",
                    "CONTAINER SIZE=%u\n", CONTAINER_SIZE (cb.container)));
        /* Close the file */
        if (fp)
            fclose (fp);
    }

    return;
}
